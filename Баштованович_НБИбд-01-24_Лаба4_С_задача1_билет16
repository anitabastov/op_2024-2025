#include <stdio.h>

int main() {
    int n;
    long long product = 1; 

    printf("Введите число n: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; ++i) {
        product *= i * i * i; 
    }

    printf("Произведение кубов чисел от 1 до %d равно %lld\n", n, product);
    
    return 0;
}
